# Outputs

This folder is **gitignored** — contents are not tracked in git.

Generated artifacts (figures, tables, model outputs, processed datasets) belong here. The durable copy is on {{MIRROR_SHORT}}, under `outputs/`; back new files up with `python ~/Projects/sync-mirror.py personal/family/brawl-counters --push`.

If an output is small and worth versioning (e.g., a single canonical figure for a paper), check it in explicitly and document below.
